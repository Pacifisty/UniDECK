export enum DocumentStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  WAITING_SIGNATURE = 'waiting_signature',
  SIGNED = 'signed',
  COMPLETED = 'completed',
  ARCHIVED = 'archived',
  RETURNED = 'returned',
}
